<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.idle.shoppingmall.mapper.ProductMapper">
    <insert id="saveProduct">
        insert into product(pd_name, pd_price, brand_id, pd_category, created_at)
        values ( #{product.pd_name}, #{product.pd_price}, #{product.brand_id}, #{product.pd_category}
               ,#{product.created_at})
    </insert>
</mapper>